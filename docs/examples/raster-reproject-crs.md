# Example: Raster Reproject

Use `Raster Reproject` with:

- source raster as local or remote dataset
- `Destination CRS = EPSG:2056` or another target CRS
- optional target resolution or target size
- resampling method set explicitly

Typical use:

- standardize incoming rasters into one target CRS before downstream processing
