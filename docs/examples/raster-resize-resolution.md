# Example: Raster Resize

Use `Raster Resize` with:

- `Resize by = SIZE` for explicit width/height
- or `Resize by = RESOLUTION` for target ground resolution
- output format plus optional creation options

Typical use:

- prepare lighter preview rasters or harmonize raster cell size before export
